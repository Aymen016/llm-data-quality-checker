# Optional — the checker runs a heuristic ruleset with no key (--no-llm).
# For LLM-proposed rules, set a provider + key.
LLM_PROVIDER=anthropic          # anthropic | openai | none
ANTHROPIC_API_KEY=
OPENAI_API_KEY=

ANTHROPIC_MODEL=claude-sonnet-4-6
OPENAI_MODEL=gpt-4o-mini
